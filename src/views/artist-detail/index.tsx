import React, { memo } from "react";
import { ArtistDetailWrapper } from "./style";

const ArtistDetail = memo(() => {
    return <ArtistDetailWrapper>ArtistDetail</ArtistDetailWrapper>;
});

export default ArtistDetail;
